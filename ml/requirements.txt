# HealthGate ML — Dépendances Python (entraînement modèle uniquement)
# Projet HealthGate | Centrale Casablanca | PLBD-4 | 2025-2026
#
# Usage : pour ré-entraîner le modèle Random Forest hors du serveur FastAPI.
# Le serveur FastAPI utilise backend/requirements.txt (inclut déjà scikit-learn).
#
#   pip install -r ml/requirements.txt
#   python ml/model_trainer.py

# ── Machine Learning ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0

# ── IA Groq (questions adaptatives + scan CIN) ───────────────────────────────
groq>=0.9.0
requests>=2.31.0

# ── Utilitaires ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
